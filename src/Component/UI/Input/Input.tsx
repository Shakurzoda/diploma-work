import styles from './Input.modal.css'
import { InputProps } from './Input.props';

const Input = () => {

        return (
        <input type="text" className={styles.input}/>
    );
};

export default Input;